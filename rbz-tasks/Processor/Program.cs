using System;
using System.Collections;
using System.Text;
using Dice;
using GardenFence;
using KampArrayNumber;
using MagicNumber;
using Practise;
using Quiz;
using Skytale;
using TaskSystem;

namespace Processor
{
    internal static class Program
    {
        public static void Main()
        {
            // var processor = new TaskProcessor();
            // processor.AddTask(new PracticeTaskProcessor());
            // processor.AddTask(new GardenFenceTaskProcessor());
            // processor.AddTask(new DiceTaskProcess());
            // processor.AddTask(new SkytaleTaskProcessor());
            // processor.AddTask(new MagicNumberTaskProcessor());
            // processor.AddTask(new QuizTaskProcessor());
            // processor.AddTask(new KampArrayNumberTaskProcessor());
            
            // processor.Process();
        }
    }
}